import React, { useState } from 'react';
import { ArrowLeft, TrendingUp, DollarSign, Clock, CheckCircle, XCircle, AlertCircle, ChevronRight, Copy, Share2, BarChart3, Calendar, Package, Eye, ShoppingBag, Gift, Wallet, CreditCard, Info, HelpCircle } from 'lucide-react';

const EarningsPage = () => {
  const [activeTab, setActiveTab] = useState('overview');
  const [showWithdrawModal, setShowWithdrawModal] = useState(false);
  const [withdrawAmount, setWithdrawAmount] = useState('');

  // 收益概览数据
  const earnings = {
    available: 128.50,
    frozen: 45.80,
    withdrawn: 256.00,
    total: 430.30,
    thisMonth: 89.60,
    todayEstimate: 12.30
  };

  // 数据统计
  const stats = {
    clickCount: 456,
    orderCount: 23,
    conversionRate: 5.04,
    avgCommission: 5.59
  };

  // 订单列表
  const orders = [
    {
      id: 1,
      product: 'Comotomo奶瓶250ml',
      image: '🍼',
      orderId: 'TB202412230001',
      orderAmount: 128,
      commission: 12.80,
      commissionRate: 10,
      status: 'settled',
      time: '2小时前',
      buyerNote: '3个月宝宝妈妈',
      settleTime: '预计1月20日结算'
    },
    {
      id: 2,
      product: 'Dr.Brown防胀气奶瓶',
      image: '🍼',
      orderId: 'TB202412220045',
      orderAmount: 98,
      commission: 9.80,
      commissionRate: 10,
      status: 'confirmed',
      time: '1天前',
      settleTime: '预计1月19日结算'
    },
    {
      id: 3,
      product: 'NUK辅食碗套装',
      image: '🥣',
      orderId: 'TB202412210023',
      orderAmount: 68,
      commission: 6.80,
      commissionRate: 10,
      status: 'paid',
      time: '2天前',
      settleTime: '等待确认收货'
    },
    {
      id: 4,
      product: 'babycare湿巾10包',
      image: '🧻',
      orderId: 'TB202412200012',
      orderAmount: 45,
      commission: 4.50,
      commissionRate: 10,
      status: 'refunded',
      time: '3天前',
      refundReason: '买家退款'
    }
  ];

  // 提现记录
  const withdrawals = [
    {
      id: 1,
      amount: 100,
      status: 'success',
      time: '2024-12-20 14:30',
      arriveTime: '2024-12-21 10:15',
      method: '微信',
      fee: 0
    },
    {
      id: 2,
      amount: 156,
      status: 'processing',
      time: '2024-12-23 16:20',
      method: '微信',
      fee: 0
    }
  ];

  // 推广数据
  const promotionData = [
    { date: '12-17', clicks: 45, orders: 2, earnings: 15.60 },
    { date: '12-18', clicks: 52, orders: 3, earnings: 22.40 },
    { date: '12-19', clicks: 38, orders: 1, earnings: 8.90 },
    { date: '12-20', clicks: 61, orders: 4, earnings: 28.50 },
    { date: '12-21', clicks: 48, orders: 2, earnings: 16.20 },
    { date: '12-22', clicks: 55, orders: 3, earnings: 24.80 },
    { date: '12-23', clicks: 67, orders: 4, earnings: 32.70 }
  ];

  const getStatusInfo = (status) => {
    const statusMap = {
      settled: { text: '已结算', color: 'text-green-600', bg: 'bg-green-50' },
      confirmed: { text: '已确认', color: 'text-blue-600', bg: 'bg-blue-50' },
      paid: { text: '已付款', color: 'text-orange-600', bg: 'bg-orange-50' },
      refunded: { text: '已退款', color: 'text-red-600', bg: 'bg-red-50' }
    };
    return statusMap[status] || statusMap.paid;
  };

  const handleWithdraw = () => {
    setShowWithdrawModal(true);
  };

  const confirmWithdraw = () => {
    // 提现逻辑
    setShowWithdrawModal(false);
    setWithdrawAmount('');
  };

  const renderHeader = () => (
    <div className="bg-gradient-to-r from-green-500 to-emerald-500 px-4 pt-4 pb-6 text-white">
      <div className="flex items-center justify-between mb-4">
        <button className="flex items-center gap-2">
          <ArrowLeft className="w-5 h-5" />
          <span className="font-bold text-lg">我的收益</span>
        </button>
        <button className="flex items-center gap-1 text-sm">
          <HelpCircle className="w-4 h-4" />
          <span>收益说明</span>
        </button>
      </div>

      {/* 收益卡片 */}
      <div className="bg-white/20 backdrop-blur-sm rounded-2xl p-4">
        <div className="flex items-center justify-between mb-4">
          <div>
            <div className="text-sm opacity-90 mb-1">可提现金额（元）</div>
            <div className="text-4xl font-bold">¥{earnings.available.toFixed(2)}</div>
          </div>
          <button
            onClick={handleWithdraw}
            disabled={earnings.available < 10}
            className={`px-6 py-2 rounded-xl font-semibold ${
              earnings.available >= 10
                ? 'bg-white text-green-600'
                : 'bg-white/30 text-white/60'
            }`}
          >
            提现
          </button>
        </div>

        <div className="grid grid-cols-3 gap-3 pt-3 border-t border-white/30">
          <div>
            <div className="text-lg font-bold">¥{earnings.frozen.toFixed(2)}</div>
            <div className="text-xs opacity-80">冻结中</div>
          </div>
          <div>
            <div className="text-lg font-bold">¥{earnings.withdrawn.toFixed(2)}</div>
            <div className="text-xs opacity-80">已提现</div>
          </div>
          <div>
            <div className="text-lg font-bold">¥{earnings.total.toFixed(2)}</div>
            <div className="text-xs opacity-80">累计收益</div>
          </div>
        </div>
      </div>

      {/* 今日数据 */}
      <div className="mt-3 flex gap-3">
        <div className="flex-1 bg-white/10 backdrop-blur-sm rounded-xl p-3">
          <div className="text-xs opacity-80 mb-1">今日预估</div>
          <div className="text-xl font-bold">¥{earnings.todayEstimate.toFixed(2)}</div>
        </div>
        <div className="flex-1 bg-white/10 backdrop-blur-sm rounded-xl p-3">
          <div className="text-xs opacity-80 mb-1">本月累计</div>
          <div className="text-xl font-bold">¥{earnings.thisMonth.toFixed(2)}</div>
        </div>
      </div>
    </div>
  );

  const renderOverview = () => (
    <div className="p-4 space-y-4">
      {/* 数据统计 */}
      <div className="bg-white rounded-2xl p-4 border border-gray-200">
        <h3 className="font-bold text-gray-800 mb-3">推广数据</h3>
        <div className="grid grid-cols-2 gap-3">
          <div className="bg-blue-50 rounded-xl p-3">
            <div className="flex items-center gap-2 mb-1">
              <Eye className="w-4 h-4 text-blue-600" />
              <span className="text-xs text-gray-600">点击量</span>
            </div>
            <div className="text-2xl font-bold text-blue-600">{stats.clickCount}</div>
          </div>
          <div className="bg-green-50 rounded-xl p-3">
            <div className="flex items-center gap-2 mb-1">
              <ShoppingBag className="w-4 h-4 text-green-600" />
              <span className="text-xs text-gray-600">订单数</span>
            </div>
            <div className="text-2xl font-bold text-green-600">{stats.orderCount}</div>
          </div>
          <div className="bg-purple-50 rounded-xl p-3">
            <div className="flex items-center gap-2 mb-1">
              <TrendingUp className="w-4 h-4 text-purple-600" />
              <span className="text-xs text-gray-600">转化率</span>
            </div>
            <div className="text-2xl font-bold text-purple-600">{stats.conversionRate}%</div>
          </div>
          <div className="bg-orange-50 rounded-xl p-3">
            <div className="flex items-center gap-2 mb-1">
              <DollarSign className="w-4 h-4 text-orange-600" />
              <span className="text-xs text-gray-600">平均佣金</span>
            </div>
            <div className="text-2xl font-bold text-orange-600">¥{stats.avgCommission}</div>
          </div>
        </div>
      </div>

      {/* 近7日趋势 */}
      <div className="bg-white rounded-2xl p-4 border border-gray-200">
        <div className="flex items-center justify-between mb-3">
          <h3 className="font-bold text-gray-800">近7日收益趋势</h3>
          <button className="text-xs text-blue-600">查看详情 →</button>
        </div>
        
        {/* 简易柱状图 */}
        <div className="flex items-end justify-between gap-1 h-32 mb-2">
          {promotionData.map((data, index) => {
            const maxEarnings = Math.max(...promotionData.map(d => d.earnings));
            const height = (data.earnings / maxEarnings) * 100;
            return (
              <div key={index} className="flex-1 flex flex-col items-center">
                <div className="relative w-full">
                  <div
                    className="w-full bg-gradient-to-t from-green-500 to-emerald-400 rounded-t"
                    style={{ height: `${height}px` }}
                  ></div>
                </div>
                <div className="text-xs text-gray-500 mt-1">{data.date.slice(3)}</div>
              </div>
            );
          })}
        </div>

        <div className="bg-gray-50 rounded-lg p-2 text-xs text-gray-600">
          <div className="flex items-center justify-between">
            <span>7日总收益</span>
            <span className="font-bold text-green-600">
              ¥{promotionData.reduce((sum, d) => sum + d.earnings, 0).toFixed(2)}
            </span>
          </div>
        </div>
      </div>

      {/* 推广工具 */}
      <div className="bg-gradient-to-r from-blue-50 to-purple-50 rounded-2xl p-4 border border-blue-200">
        <div className="flex items-center gap-2 mb-3">
          <Share2 className="w-5 h-5 text-blue-600" />
          <h3 className="font-bold text-gray-800">推广工具</h3>
        </div>
        <div className="space-y-2">
          <button className="w-full bg-white rounded-xl p-3 flex items-center justify-between hover:bg-gray-50 transition-all">
            <div className="flex items-center gap-3">
              <div className="w-10 h-10 bg-blue-100 rounded-lg flex items-center justify-center">
                <Share2 className="w-5 h-5 text-blue-600" />
              </div>
              <div className="text-left">
                <div className="font-medium text-sm text-gray-800">生成推广链接</div>
                <div className="text-xs text-gray-500">分享产品赚佣金</div>
              </div>
            </div>
            <ChevronRight className="w-5 h-5 text-gray-400" />
          </button>
          <button className="w-full bg-white rounded-xl p-3 flex items-center justify-between hover:bg-gray-50 transition-all">
            <div className="flex items-center gap-3">
              <div className="w-10 h-10 bg-purple-100 rounded-lg flex items-center justify-center">
                <BarChart3 className="w-5 h-5 text-purple-600" />
              </div>
              <div className="text-left">
                <div className="font-medium text-sm text-gray-800">查看推广排行</div>
                <div className="text-xs text-gray-500">看看大家都在推什么</div>
              </div>
            </div>
            <ChevronRight className="w-5 h-5 text-gray-400" />
          </button>
        </div>
      </div>
    </div>
  );

  const renderOrders = () => (
    <div className="p-4 space-y-3">
      {/* 筛选栏 */}
      <div className="flex items-center gap-2 overflow-x-auto pb-2">
        <button className="px-3 py-1 bg-green-500 text-white text-xs rounded-full whitespace-nowrap">
          全部({orders.length})
        </button>
        <button className="px-3 py-1 bg-gray-100 text-gray-700 text-xs rounded-full whitespace-nowrap">
          已结算
        </button>
        <button className="px-3 py-1 bg-gray-100 text-gray-700 text-xs rounded-full whitespace-nowrap">
          待结算
        </button>
        <button className="px-3 py-1 bg-gray-100 text-gray-700 text-xs rounded-full whitespace-nowrap">
          已失效
        </button>
      </div>

      {/* 订单列表 */}
      {orders.map((order) => {
        const statusInfo = getStatusInfo(order.status);
        return (
          <div key={order.id} className="bg-white rounded-2xl p-4 border border-gray-200">
            {/* 订单头部 */}
            <div className="flex items-center justify-between mb-3">
              <div className="flex items-center gap-2">
                <Clock className="w-4 h-4 text-gray-400" />
                <span className="text-xs text-gray-500">{order.time}</span>
              </div>
              <div className={`text-xs px-2 py-1 rounded ${statusInfo.bg} ${statusInfo.color} font-medium`}>
                {statusInfo.text}
              </div>
            </div>

            {/* 商品信息 */}
            <div className="flex gap-3 mb-3">
              <div className="w-16 h-16 bg-gray-100 rounded-lg flex items-center justify-center text-3xl flex-shrink-0">
                {order.image}
              </div>
              <div className="flex-1">
                <div className="font-medium text-sm text-gray-800 mb-1 line-clamp-2">
                  {order.product}
                </div>
                <div className="text-xs text-gray-500">
                  订单号：{order.orderId}
                </div>
                {order.buyerNote && (
                  <div className="text-xs text-blue-600 mt-1">
                    👤 {order.buyerNote}
                  </div>
                )}
              </div>
            </div>

            {/* 金额信息 */}
            <div className="bg-gray-50 rounded-lg p-3 mb-3">
              <div className="flex items-center justify-between mb-2">
                <span className="text-xs text-gray-600">订单金额</span>
                <span className="text-sm font-semibold text-gray-800">¥{order.orderAmount}</span>
              </div>
              <div className="flex items-center justify-between">
                <span className="text-xs text-gray-600">预估佣金（{order.commissionRate}%）</span>
                <span className="text-lg font-bold text-green-600">¥{order.commission.toFixed(2)}</span>
              </div>
            </div>

            {/* 状态说明 */}
            {order.status === 'refunded' ? (
              <div className="flex items-center gap-1 text-xs text-red-600">
                <XCircle className="w-3 h-3" />
                <span>{order.refundReason}</span>
              </div>
            ) : (
              <div className="flex items-center gap-1 text-xs text-gray-500">
                <AlertCircle className="w-3 h-3" />
                <span>{order.settleTime}</span>
              </div>
            )}
          </div>
        );
      })}

      {/* 加载更多 */}
      <div className="text-center py-4">
        <button className="text-sm text-gray-600">
          查看更多订单 →
        </button>
      </div>
    </div>
  );

  const renderWithdrawals = () => (
    <div className="p-4 space-y-3">
      {/* 提现说明 */}
      <div className="bg-blue-50 border border-blue-200 rounded-xl p-3">
        <div className="flex items-start gap-2">
          <Info className="w-4 h-4 text-blue-600 flex-shrink-0 mt-0.5" />
          <div className="text-xs text-blue-800">
            <div className="font-semibold mb-1">提现说明</div>
            <div>• 最低提现金额：10元</div>
            <div>• 每月可提现4次</div>
            <div>• 到账时间：1-3个工作日</div>
            <div>• 提现免手续费</div>
          </div>
        </div>
      </div>

      {/* 提现记录 */}
      {withdrawals.map((record) => (
        <div key={record.id} className="bg-white rounded-2xl p-4 border border-gray-200">
          <div className="flex items-start justify-between mb-3">
            <div className="flex items-center gap-3">
              <div className={`w-10 h-10 rounded-lg flex items-center justify-center ${
                record.status === 'success' ? 'bg-green-100' : 'bg-orange-100'
              }`}>
                <Wallet className={`w-5 h-5 ${
                  record.status === 'success' ? 'text-green-600' : 'text-orange-600'
                }`} />
              </div>
              <div>
                <div className="font-bold text-lg text-gray-800">¥{record.amount.toFixed(2)}</div>
                <div className="text-xs text-gray-500">提现到{record.method}</div>
              </div>
            </div>
            <div className={`text-xs px-2 py-1 rounded ${
              record.status === 'success' 
                ? 'bg-green-50 text-green-600' 
                : 'bg-orange-50 text-orange-600'
            }`}>
              {record.status === 'success' ? '已到账' : '处理中'}
            </div>
          </div>

          <div className="space-y-1 text-xs text-gray-500">
            <div className="flex items-center justify-between">
              <span>申请时间</span>
              <span>{record.time}</span>
            </div>
            {record.arriveTime && (
              <div className="flex items-center justify-between">
                <span>到账时间</span>
                <span>{record.arriveTime}</span>
              </div>
            )}
            <div className="flex items-center justify-between">
              <span>手续费</span>
              <span>¥{record.fee.toFixed(2)}</span>
            </div>
          </div>
        </div>
      ))}
    </div>
  );

  const renderWithdrawModal = () => {
    if (!showWithdrawModal) return null;

    return (
      <div className="fixed inset-0 bg-black/50 flex items-end justify-center z-50">
        <div className="bg-white rounded-t-3xl w-full max-w-md">
          <div className="p-6">
            <div className="flex items-center justify-between mb-6">
              <h3 className="text-lg font-bold text-gray-800">申请提现</h3>
              <button onClick={() => setShowWithdrawModal(false)}>
                <XCircle className="w-6 h-6 text-gray-400" />
              </button>
            </div>

            {/* 可提现金额 */}
            <div className="bg-green-50 rounded-xl p-4 mb-4">
              <div className="text-sm text-gray-600 mb-1">可提现金额</div>
              <div className="text-3xl font-bold text-green-600">¥{earnings.available.toFixed(2)}</div>
            </div>

            {/* 提现金额输入 */}
            <div className="mb-4">
              <label className="text-sm text-gray-700 mb-2 block">提现金额</label>
              <div className="relative">
                <input
                  type="number"
                  value={withdrawAmount}
                  onChange={(e) => setWithdrawAmount(e.target.value)}
                  placeholder="请输入提现金额"
                  className="w-full px-4 py-3 border-2 border-gray-200 rounded-xl focus:outline-none focus:border-green-500 text-lg"
                />
                <button
                  onClick={() => setWithdrawAmount(earnings.available.toFixed(2))}
                  className="absolute right-3 top-1/2 -translate-y-1/2 text-sm text-green-600"
                >
                  全部
                </button>
              </div>
              <div className="text-xs text-gray-500 mt-1">
                最低提现金额10元，当前可提现次数：3次
              </div>
            </div>

            {/* 提现方式 */}
            <div className="mb-4">
              <label className="text-sm text-gray-700 mb-2 block">提现方式</label>
              <button className="w-full bg-gray-50 rounded-xl p-4 flex items-center justify-between border-2 border-green-500">
                <div className="flex items-center gap-3">
                  <div className="w-10 h-10 bg-green-100 rounded-lg flex items-center justify-center">
                    <CreditCard className="w-5 h-5 text-green-600" />
                  </div>
                  <div className="text-left">
                    <div className="font-medium text-sm text-gray-800">微信</div>
                    <div className="text-xs text-gray-500">预计1-3个工作日到账</div>
                  </div>
                </div>
                <CheckCircle className="w-5 h-5 text-green-600" />
              </button>
            </div>

            {/* 提现按钮 */}
            <button
              onClick={confirmWithdraw}
              disabled={!withdrawAmount || parseFloat(withdrawAmount) < 10}
              className={`w-full py-3 rounded-xl font-bold ${
                withdrawAmount && parseFloat(withdrawAmount) >= 10
                  ? 'bg-gradient-to-r from-green-500 to-emerald-500 text-white'
                  : 'bg-gray-200 text-gray-400'
              }`}
            >
              确认提现
            </button>
          </div>
        </div>
      </div>
    );
  };

  return (
    <div className="max-w-md mx-auto bg-gray-50 min-h-screen flex flex-col">
      {/* 顶部收益信息 */}
      {renderHeader()}

      {/* Tab导航 */}
      <div className="bg-white px-4 flex gap-6 border-b sticky top-0 z-40">
        <button
          onClick={() => setActiveTab('overview')}
          className={`pb-3 font-medium transition-all ${
            activeTab === 'overview'
              ? 'text-green-600 border-b-2 border-green-600'
              : 'text-gray-600'
          }`}
        >
          概览
        </button>
        <button
          onClick={() => setActiveTab('orders')}
          className={`pb-3 font-medium transition-all ${
            activeTab === 'orders'
              ? 'text-green-600 border-b-2 border-green-600'
              : 'text-gray-600'
          }`}
        >
          订单明细
        </button>
        <button
          onClick={() => setActiveTab('withdrawals')}
          className={`pb-3 font-medium transition-all ${
            activeTab === 'withdrawals'
              ? 'text-green-600 border-b-2 border-green-600'
              : 'text-gray-600'
          }`}
        >
          提现记录
        </button>
      </div>

      {/* 内容区域 */}
      <div className="flex-1 overflow-y-auto pb-20">
        {activeTab === 'overview' && renderOverview()}
        {activeTab === 'orders' && renderOrders()}
        {activeTab === 'withdrawals' && renderWithdrawals()}
      </div>

      {/* 提现弹窗 */}
      {renderWithdrawModal()}
    </div>
  );
};

export default EarningsPage;
