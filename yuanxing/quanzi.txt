import React, { useState } from 'react';
import { ArrowLeft, Users, TrendingUp, MessageSquare, Heart, Share2, ChevronRight, Star, Clock, Pin, Award, Calendar, Image, Video, Gift, Sparkles, Bell, Search, Filter } from 'lucide-react';

const AgeCirclePage = () => {
  const [activeTab, setActiveTab] = useState('all');
  const [showMilestone, setShowMilestone] = useState(true);

  // 圈子信息
  const circleInfo = {
    name: '2024年9月宝宝圈',
    birthMonth: '2024年9月',
    currentAge: '3个月',
    memberCount: 2845,
    todayPosts: 156,
    todayActive: 892,
    rank: 3,
    joinedDays: 89
  };

  // 圈子活跃榜
  const activeUsers = [
    { id: 1, name: '小雨妈妈', avatar: '👩', level: 'Lv5', posts: 89, rank: 1, badge: '👑' },
    { id: 2, name: '晴天妈妈', avatar: '👱‍♀️', level: 'Lv4', posts: 67, rank: 2, badge: '🥈' },
    { id: 3, name: '暖暖妈咪', avatar: '🙋‍♀️', level: 'Lv4', posts: 54, rank: 3, badge: '🥉' }
  ];

  // 本月里程碑
  const milestones = [
    {
      id: 1,
      title: '第一次翻身',
      icon: '🤸',
      completedCount: 456,
      totalCount: 2845,
      percentage: 16,
      recentUsers: ['小雨妈妈', '晴天妈妈', '暖暖妈咪'],
      tips: '3个月左右开始，侧卧练习很重要'
    },
    {
      id: 2,
      title: '认识妈妈',
      icon: '😊',
      completedCount: 1234,
      totalCount: 2845,
      percentage: 43,
      recentUsers: ['萌萌妈', '乐乐妈'],
      tips: '看到妈妈会笑，听到声音会转头'
    },
    {
      id: 3,
      title: '抓握玩具',
      icon: '🤲',
      completedCount: 892,
      totalCount: 2845,
      percentage: 31,
      recentUsers: ['甜甜妈', '壮壮妈'],
      tips: '可以准备摇铃等易抓握玩具'
    }
  ];

  // 圈子公告
  const announcements = [
    {
      id: 1,
      title: '【本周话题】3个月宝宝睡眠倒退怎么办？',
      type: 'topic',
      time: '2小时前',
      hot: true
    },
    {
      id: 2,
      title: '【圈子活动】晒出宝宝第一次翻身视频赢好礼',
      type: 'activity',
      time: '1天前',
      deadline: '还剩3天'
    }
  ];

  // 热门话题
  const hotTopics = [
    { id: 1, name: '宝宝睡眠', icon: '😴', posts: 234, hot: true },
    { id: 2, name: '辅食添加', icon: '🍚', posts: 189 },
    { id: 3, name: '翻身训练', icon: '🤸', posts: 167 },
    { id: 4, name: '玩具推荐', icon: '🧸', posts: 145 }
  ];

  // 帖子列表
  const posts = [
    {
      id: 1,
      type: 'milestone',
      user: {
        name: '小雨妈妈',
        avatar: '👩',
        level: 'Lv5',
        isTopUser: true
      },
      content: '宝宝今天第一次翻身成功了！激动到哭😭 从侧卧练习了两周，今天终于自己翻过去了！',
      images: ['📸', '📸', '📸'],
      milestone: '第一次翻身',
      time: '10分钟前',
      likes: 89,
      comments: 34,
      shares: 12,
      isHot: true,
      isPinned: false
    },
    {
      id: 2,
      type: 'question',
      user: {
        name: '晴天妈妈',
        avatar: '👱‍♀️',
        level: 'Lv3'
      },
      content: '宝宝最近睡眠倒退严重，晚上醒好几次，白天也不好好睡，姐妹们有什么办法吗？',
      topic: '宝宝睡眠',
      time: '1小时前',
      likes: 45,
      comments: 23,
      hasAcceptedAnswer: false,
      reward: 50
    },
    {
      id: 3,
      type: 'share',
      user: {
        name: '暖暖妈咪',
        avatar: '🙋‍♀️',
        level: 'Lv4'
      },
      content: '分享一下我家宝宝最喜欢的玩具，这个摇铃真的很好用，宝宝能抓握半小时不松手',
      images: ['📸', '📸'],
      product: {
        name: 'Fisher-Price摇铃',
        price: 59,
        rating: 4.8
      },
      time: '2小时前',
      likes: 67,
      comments: 19,
      shares: 8,
      isRecommend: true
    },
    {
      id: 4,
      type: 'daily',
      user: {
        name: '萌萌妈',
        avatar: '👩‍🦰',
        level: 'Lv2'
      },
      content: '今天宝宝情绪特别好，对着我一直笑，感觉被治愈了💕',
      video: true,
      time: '3小时前',
      likes: 156,
      comments: 45,
      shares: 23
    },
    {
      id: 5,
      type: 'help',
      user: {
        name: '甜甜妈',
        avatar: '👱',
        level: 'Lv3'
      },
      content: '求助！宝宝最近不爱吃奶了，每次喂奶都要哄很久，担心营养不够怎么办？',
      topic: '喂养问题',
      time: '5小时前',
      likes: 34,
      comments: 18,
      hasAcceptedAnswer: true
    }
  ];

  const renderCircleHeader = () => (
    <div className="bg-gradient-to-r from-purple-500 to-pink-500 px-4 pt-4 pb-6 text-white">
      <div className="flex items-center justify-between mb-4">
        <button className="flex items-center gap-2">
          <ArrowLeft className="w-5 h-5" />
        </button>
        <div className="flex items-center gap-3">
          <button className="relative">
            <Bell className="w-5 h-5" />
            <div className="absolute -top-1 -right-1 w-2 h-2 bg-red-500 rounded-full"></div>
          </button>
          <button>
            <Search className="w-5 h-5" />
          </button>
        </div>
      </div>

      {/* 圈子基础信息 */}
      <div className="mb-4">
        <div className="flex items-center gap-2 mb-2">
          <h1 className="text-2xl font-bold">{circleInfo.name}</h1>
          <div className="bg-white/30 text-xs px-2 py-1 rounded-full">
            #{circleInfo.rank}活跃圈
          </div>
        </div>
        <div className="text-sm opacity-90">
          宝宝现在{circleInfo.currentAge} • 已加入{circleInfo.joinedDays}天
        </div>
      </div>

      {/* 数据统计 */}
      <div className="grid grid-cols-3 gap-3">
        <div className="bg-white/20 backdrop-blur-sm rounded-xl p-3 text-center">
          <div className="text-2xl font-bold mb-1">{circleInfo.memberCount.toLocaleString()}</div>
          <div className="text-xs opacity-90">圈子成员</div>
        </div>
        <div className="bg-white/20 backdrop-blur-sm rounded-xl p-3 text-center">
          <div className="text-2xl font-bold mb-1">{circleInfo.todayPosts}</div>
          <div className="text-xs opacity-90">今日新帖</div>
        </div>
        <div className="bg-white/20 backdrop-blur-sm rounded-xl p-3 text-center">
          <div className="text-2xl font-bold mb-1">{circleInfo.todayActive}</div>
          <div className="text-xs opacity-90">今日活跃</div>
        </div>
      </div>
    </div>
  );

  const renderMilestoneCard = () => (
    showMilestone && (
      <div className="bg-gradient-to-br from-yellow-50 to-orange-50 rounded-2xl p-4 border-2 border-yellow-200 mb-4">
        <div className="flex items-center justify-between mb-3">
          <div className="flex items-center gap-2">
            <Sparkles className="w-5 h-5 text-yellow-600" />
            <h3 className="font-bold text-gray-800">本月成长里程碑</h3>
          </div>
          <button onClick={() => setShowMilestone(false)} className="text-gray-400 text-sm">
            收起
          </button>
        </div>

        <div className="space-y-3">
          {milestones.map((milestone) => (
            <div key={milestone.id} className="bg-white rounded-xl p-3">
              <div className="flex items-start gap-3 mb-2">
                <div className="text-3xl">{milestone.icon}</div>
                <div className="flex-1">
                  <div className="font-semibold text-gray-800 mb-1">{milestone.title}</div>
                  <div className="text-xs text-gray-600 mb-2">{milestone.tips}</div>
                  <div className="flex items-center gap-2 mb-2">
                    <div className="flex-1 bg-gray-200 rounded-full h-2 overflow-hidden">
                      <div 
                        className="bg-gradient-to-r from-yellow-400 to-orange-400 h-full"
                        style={{ width: `${milestone.percentage}%` }}
                      ></div>
                    </div>
                    <span className="text-xs text-gray-600 font-semibold">{milestone.percentage}%</span>
                  </div>
                  <div className="flex items-center justify-between">
                    <div className="text-xs text-gray-500">
                      {milestone.completedCount}/{milestone.totalCount}位宝宝达成
                    </div>
                    <button className="text-xs text-blue-600 font-medium">
                      我也达成了 →
                    </button>
                  </div>
                </div>
              </div>
            </div>
          ))}
        </div>

        <button className="w-full mt-3 text-sm text-gray-600 py-2 border border-gray-200 rounded-lg bg-white">
          查看所有里程碑 →
        </button>
      </div>
    )
  );

  const renderAnnouncementsCard = () => (
    <div className="bg-blue-50 rounded-2xl p-4 border border-blue-200 mb-4">
      <div className="flex items-center gap-2 mb-3">
        <Pin className="w-4 h-4 text-blue-600" />
        <h3 className="font-bold text-gray-800">圈子公告</h3>
      </div>
      <div className="space-y-2">
        {announcements.map((announcement) => (
          <div key={announcement.id} className="bg-white rounded-xl p-3 hover:bg-gray-50 transition-all cursor-pointer">
            <div className="flex items-start justify-between">
              <div className="flex-1">
                <div className="flex items-center gap-2 mb-1">
                  <span className={`text-xs px-2 py-0.5 rounded ${
                    announcement.type === 'topic'
                      ? 'bg-purple-100 text-purple-700'
                      : 'bg-orange-100 text-orange-700'
                  }`}>
                    {announcement.type === 'topic' ? '话题' : '活动'}
                  </span>
                  {announcement.hot && (
                    <span className="text-xs bg-red-500 text-white px-2 py-0.5 rounded">
                      热
                    </span>
                  )}
                </div>
                <div className="text-sm font-medium text-gray-800 mb-1">{announcement.title}</div>
                <div className="flex items-center gap-2 text-xs text-gray-500">
                  <span>{announcement.time}</span>
                  {announcement.deadline && (
                    <>
                      <span>•</span>
                      <span className="text-orange-600">{announcement.deadline}</span>
                    </>
                  )}
                </div>
              </div>
              <ChevronRight className="w-5 h-5 text-gray-400 flex-shrink-0" />
            </div>
          </div>
        ))}
      </div>
    </div>
  );

  const renderHotTopics = () => (
    <div className="mb-4">
      <div className="flex items-center justify-between mb-3">
        <h3 className="font-bold text-gray-800">热门话题</h3>
        <button className="text-sm text-blue-600">更多 →</button>
      </div>
      <div className="flex gap-2 overflow-x-auto pb-2">
        {hotTopics.map((topic) => (
          <button
            key={topic.id}
            className="flex items-center gap-2 px-3 py-2 bg-white rounded-xl border border-gray-200 whitespace-nowrap hover:border-blue-500 transition-all relative"
          >
            {topic.hot && (
              <div className="absolute -top-1 -right-1 bg-red-500 text-white text-xs px-1.5 py-0.5 rounded-full">
                热
              </div>
            )}
            <span className="text-xl">{topic.icon}</span>
            <div className="text-left">
              <div className="text-sm font-medium text-gray-800">{topic.name}</div>
              <div className="text-xs text-gray-500">{topic.posts}条</div>
            </div>
          </button>
        ))}
      </div>
    </div>
  );

  const renderActiveRank = () => (
    <div className="bg-gradient-to-br from-purple-50 to-pink-50 rounded-2xl p-4 border border-purple-200 mb-4">
      <div className="flex items-center gap-2 mb-3">
        <Award className="w-5 h-5 text-purple-600" />
        <h3 className="font-bold text-gray-800">本月活跃榜</h3>
        <span className="text-xs text-purple-600 bg-purple-100 px-2 py-0.5 rounded-full ml-auto">
          TOP 3
        </span>
      </div>
      <div className="space-y-2">
        {activeUsers.map((user) => (
          <div key={user.id} className="bg-white rounded-xl p-3 flex items-center gap-3">
            <div className="text-2xl">{user.badge}</div>
            <div className="text-2xl">{user.avatar}</div>
            <div className="flex-1">
              <div className="flex items-center gap-2">
                <span className="font-semibold text-sm">{user.name}</span>
                <span className="text-xs bg-gradient-to-r from-yellow-400 to-orange-400 text-white px-2 py-0.5 rounded">
                  {user.level}
                </span>
              </div>
              <div className="text-xs text-gray-500">{user.posts}条动态</div>
            </div>
            <button className="text-blue-600 text-sm font-medium">
              关注
            </button>
          </div>
        ))}
      </div>
      <button className="w-full mt-3 text-sm text-gray-600 py-2 border border-gray-200 rounded-lg bg-white">
        查看完整榜单 →
      </button>
    </div>
  );

  const renderPostCard = (post) => (
    <div key={post.id} className="bg-white rounded-2xl p-4 border border-gray-200 shadow-sm">
      {/* 置顶标识 */}
      {post.isPinned && (
        <div className="flex items-center gap-1 text-orange-600 text-xs mb-2">
          <Pin className="w-3 h-3" />
          <span>置顶</span>
        </div>
      )}

      {/* 用户信息 */}
      <div className="flex items-center gap-2 mb-3">
        <div className="text-2xl">{post.user.avatar}</div>
        <div className="flex-1">
          <div className="flex items-center gap-2">
            <span className="font-semibold text-sm">{post.user.name}</span>
            <span className="text-xs bg-gradient-to-r from-yellow-400 to-orange-400 text-white px-2 py-0.5 rounded">
              {post.user.level}
            </span>
            {post.user.isTopUser && (
              <span className="text-xs bg-purple-100 text-purple-700 px-2 py-0.5 rounded">
                活跃榜1
              </span>
            )}
          </div>
          <div className="text-xs text-gray-500">{post.time}</div>
        </div>
        <button className="text-gray-400">•••</button>
      </div>

      {/* 内容类型标识 */}
      <div className="flex items-center gap-2 mb-2">
        {post.type === 'milestone' && (
          <div className="flex items-center gap-1 bg-yellow-100 text-yellow-700 text-xs px-2 py-1 rounded-full">
            <Sparkles className="w-3 h-3" />
            <span>成长里程碑</span>
          </div>
        )}
        {post.type === 'question' && (
          <div className="flex items-center gap-1 bg-blue-100 text-blue-700 text-xs px-2 py-1 rounded-full">
            <MessageSquare className="w-3 h-3" />
            <span>求助</span>
          </div>
        )}
        {post.type === 'share' && post.isRecommend && (
          <div className="flex items-center gap-1 bg-green-100 text-green-700 text-xs px-2 py-1 rounded-full">
            <Star className="w-3 h-3" />
            <span>好物推荐</span>
          </div>
        )}
        {post.topic && (
          <div className="text-xs text-blue-600 bg-blue-50 px-2 py-1 rounded-full">
            #{post.topic}
          </div>
        )}
        {post.isHot && (
          <div className="text-xs bg-red-500 text-white px-2 py-1 rounded-full">
            🔥 热门
          </div>
        )}
      </div>

      {/* 里程碑特殊样式 */}
      {post.milestone && (
        <div className="bg-yellow-50 border border-yellow-200 rounded-lg p-2 mb-3">
          <div className="text-sm font-semibold text-yellow-800">
            🎉 达成：{post.milestone}
          </div>
        </div>
      )}

      {/* 帖子内容 */}
      <div className="text-sm text-gray-800 mb-3 leading-relaxed">
        {post.content}
      </div>

      {/* 图片/视频 */}
      {post.images && (
        <div className="grid grid-cols-3 gap-2 mb-3">
          {post.images.map((img, idx) => (
            <div key={idx} className="aspect-square bg-gray-100 rounded-lg flex items-center justify-center text-3xl">
              {img}
            </div>
          ))}
        </div>
      )}
      {post.video && (
        <div className="aspect-video bg-gray-100 rounded-xl flex items-center justify-center mb-3">
          <Play className="w-12 h-12 text-gray-400" />
        </div>
      )}

      {/* 关联产品 */}
      {post.product && (
        <div className="bg-gray-50 rounded-xl p-3 mb-3 flex items-center gap-3">
          <div className="w-12 h-12 bg-white rounded-lg flex items-center justify-center text-2xl">
            🍼
          </div>
          <div className="flex-1">
            <div className="text-sm font-semibold text-gray-800">{post.product.name}</div>
            <div className="flex items-center gap-2">
              <span className="text-red-500 font-bold text-sm">¥{post.product.price}</span>
              <div className="flex items-center gap-1">
                <Star className="w-3 h-3 fill-yellow-400 text-yellow-400" />
                <span className="text-xs text-gray-600">{post.product.rating}</span>
              </div>
            </div>
          </div>
          <button className="text-blue-600 text-sm font-medium">
            查看
          </button>
        </div>
      )}

      {/* 悬赏标识 */}
      {post.reward && (
        <div className="bg-orange-50 border border-orange-200 rounded-lg p-2 mb-3 flex items-center gap-2">
          <Gift className="w-4 h-4 text-orange-600" />
          <span className="text-sm text-orange-800">悬赏 {post.reward} 积分</span>
          {post.hasAcceptedAnswer && (
            <span className="text-xs bg-green-100 text-green-700 px-2 py-0.5 rounded ml-auto">
              ✓ 已解决
            </span>
          )}
        </div>
      )}

      {/* 互动栏 */}
      <div className="flex items-center justify-between pt-3 border-t">
        <button className="flex items-center gap-1 text-gray-600">
          <Heart className="w-4 h-4" />
          <span className="text-sm">{post.likes}</span>
        </button>
        <button className="flex items-center gap-1 text-gray-600">
          <MessageSquare className="w-4 h-4" />
          <span className="text-sm">{post.comments}</span>
        </button>
        <button className="flex items-center gap-1 text-gray-600">
          <Share2 className="w-4 h-4" />
          <span className="text-sm">{post.shares}</span>
        </button>
      </div>
    </div>
  );

  const renderPostsList = () => (
    <div className="space-y-3">
      {posts.map((post) => renderPostCard(post))}
    </div>
  );

  return (
    <div className="max-w-md mx-auto bg-gray-50 min-h-screen flex flex-col">
      {/* 圈子头部 */}
      {renderCircleHeader()}

      {/* Tab导航 */}
      <div className="bg-white px-4 flex gap-6 border-b sticky top-0 z-40">
        <button
          onClick={() => setActiveTab('all')}
          className={`pb-3 font-medium transition-all ${
            activeTab === 'all'
              ? 'text-purple-600 border-b-2 border-purple-600'
              : 'text-gray-600'
          }`}
        >
          全部
        </button>
        <button
          onClick={() => setActiveTab('milestone')}
          className={`pb-3 font-medium transition-all ${
            activeTab === 'milestone'
              ? 'text-purple-600 border-b-2 border-purple-600'
              : 'text-gray-600'
          }`}
        >
          里程碑
        </button>
        <button
          onClick={() => setActiveTab('help')}
          className={`pb-3 font-medium transition-all ${
            activeTab === 'help'
              ? 'text-purple-600 border-b-2 border-purple-600'
              : 'text-gray-600'
          }`}
        >
          求助
        </button>
        <button
          onClick={() => setActiveTab('share')}
          className={`pb-3 font-medium transition-all ${
            activeTab === 'share'
              ? 'text-purple-600 border-b-2 border-purple-600'
              : 'text-gray-600'
          }`}
        >
          分享
        </button>
      </div>

      {/* 内容区域 */}
      <div className="flex-1 overflow-y-auto p-4 pb-24">
        {renderMilestoneCard()}
        {renderAnnouncementsCard()}
        {renderHotTopics()}
        {renderActiveRank()}
        {renderPostsList()}
        
        {/* 加载更多 */}
        <div className="text-center py-6">
          <button className="text-sm text-gray-600">
            查看更多 →
          </button>
        </div>
      </div>

      {/* 底部发帖按钮 */}
      <div className="fixed bottom-20 right-4 z-50">
        <button className="w-14 h-14 bg-gradient-to-r from-purple-500 to-pink-500 text-white rounded-full shadow-lg flex items-center justify-center">
          <span className="text-2xl">✏️</span>
        </button>
      </div>
    </div>
  );
};

export default AgeCirclePage;
