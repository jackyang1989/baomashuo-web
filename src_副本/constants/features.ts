/**
 * 宝妈说 - 灰度配置
 * Feature Flags for Gray Release
 */

export const FEATURES = {
    // ==========================================
    // Ⅰ. 怎么选（选前决策）
    // ==========================================
    SELECT_BOTTLE: true,           // 奶瓶怎么选
    SELECT_NIPPLE: true,           // 奶嘴怎么选
    SELECT_ACCESSORIES: true,      // 配件怎么选
    SELECT_COMPARE: false,         // 品牌/型号对比（灰度）
    SELECT_ASSISTANT: false,       // 决策辅助（灰度）

    // ==========================================
    // Ⅱ. 怎么用（使用过程）
    // ==========================================
    USAGE_GUIDE: false,            // 正确使用方法（灰度）
    USAGE_PROBLEMS: false,         // 常见问题处理（灰度）
    USAGE_STAGES: false,           // 使用阶段建议（灰度）

    // ==========================================
    // Ⅲ. 值不值（复盘与评价）
    // ==========================================
    REVIEW_BASIC: true,            // 基础评价
    REVIEW_DEEP: false,            // 深度反馈（灰度）
    REVIEW_COMPARE: false,         // 对比评价（灰度）
    REVIEW_FOLLOWUP: false,        // 长期追评（灰度）

    // ==========================================
    // Ⅳ. 避坑榜（负反馈系统）
    // ==========================================
    PITFALL_AGE: true,             // 同月龄避坑榜
    PITFALL_SCENARIO: true,        // 场景避坑
    PITFALL_REASONS: false,        // 高频踩坑原因（灰度）
    PITFALL_MARKETING: false,      // 营销陷阱（灰度）

    // ==========================================
    // Ⅴ. 清单与工具（决策辅助）
    // ==========================================
    TOOLS_CHECKLIST: false,        // 同月龄必备清单（灰度）
    TOOLS_SCENARIO: false,         // 场景清单（灰度）
    TOOLS_QUICK_SELECT: false,     // 快速选择工具（灰度）

    // ==========================================
    // 💰 CPS 电商
    // ==========================================
    CPS_ENABLED: true,             // CPS 总开关
    CPS_SHOW_PRICE: true,          // 显示价格
    CPS_MULTI_PLATFORM: true,      // 多平台对比
    CPS_LABEL: true,               // 显示"合作链接"标识

    // ==========================================
    // 👤 用户参与
    // ==========================================
    USER_VOTE: true,               // 点赞/点踩
    USER_ME_TOO: true,             // 标记"我也遇到"
    USER_FAVORITE: false,          // 收藏产品（灰度）
    USER_FOLLOWUP: false,          // 追评提醒（灰度）
} as const;

/** 检查功能是否开启 */
export const isFeatureEnabled = (feature: keyof typeof FEATURES): boolean => {
    return FEATURES[feature];
};
